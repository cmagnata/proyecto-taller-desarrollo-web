
Fila_turnos[{
    id,
    tipo_turno,
    valor,
}]






//Esto debe ir dentro de la función que maneja la lógica de la creación de los numeros
function crearTurno(tipo_turno) {
    if (tipo_turno === 'General') {

    }

    else if (tipo_turno === 'Toma de muestra') {

    }

    else if (tipo_turno === 'Pagos') {

    }
}